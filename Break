'''Оператор Break'''
while True:
    s = input('Введите что-нибудь: ')
    if s == 'выход':
        break  # оператор прерывания цикла
    print('Длина строки: ', len(s))  #len - длинна строки

print('Завершение')
